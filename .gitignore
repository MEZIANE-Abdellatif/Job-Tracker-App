# Cursor — rules & local AI config
.cursor/

# Legacy single-file Cursor rules (repo root)
.cursorrules

# Local-only specs (not pushed)
ARCH.md
SECURITY.md

# Dependencies & build output (any subfolder)
node_modules/
dist/
coverage/

# Environment & OS noise
.env
*.log
.DS_Store
