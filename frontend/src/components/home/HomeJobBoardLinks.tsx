import { JobBoardLinks } from "@/components/job-boards/JobBoardLinks";

/** Home: full-width grid of outbound job board links. */
export function HomeJobBoardLinks() {
  return <JobBoardLinks variant="grid" className="mt-12 w-full" />;
}
